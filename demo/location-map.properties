# Backs StaticMapLocationResolver.fromPropertiesFile - each key is a
# ref://<id> a contract's location can reference, each value the literal
# location it resolves to. Used by dev/location-provider-demo, together with
# demo/contracts/invaract_output_location_ref.yaml, to prove
# com.invaract.sparkadapter.location.ContractLocationResolution against a
# real Spark job, not just in isolated unit tests.
demo-input=demo/input/sample.csv
