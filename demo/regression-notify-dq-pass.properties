# Notification sink config for dev/regression's data-quality PASS case - see
# demo/notify.properties for the general explanation. A separate file so
# this case's events don't mix with the other regression cases' or dev/test's.
sink.enabled=true
sink.class=com.invaract.sparkadapter.notification.FileNotificationSink
sink.property.path=demo/output/regression_dq_pass_events.jsonl
